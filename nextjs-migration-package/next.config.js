/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you need to serve static files from the data directory
  // You may need to configure this based on your deployment setup
}

module.exports = nextConfig


